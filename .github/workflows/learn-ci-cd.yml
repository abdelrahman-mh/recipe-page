name: learn-github-actions

on:
  push:
    branches:
      - main

jobs:
  lint:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: read
      statuses: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Super-linter
        uses: github/super-linter@v6.3
        env:
          RUN_LOCAL: true
